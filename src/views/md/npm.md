js
## npm
```js
npm uninstall <name> //删除模块，但不删除模块留在package.json中的信息
npm uninstall <name> --save //同时删除模块dependencies
npm uninstall <name> --save-dev //删除模块。同时删除模块devDependencies信息
```
```js
npm run build //打包生成dist文件
```